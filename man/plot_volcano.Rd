% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_volcano.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Create Volcano Plot for Differential Expression Results}
\usage{
plot_volcano(
  devil.res,
  lfc_cut = 1,
  pval_cut = 0.05,
  labels = TRUE,
  colors = c("gray", "forestgreen", "steelblue", "indianred"),
  color_alpha = 0.7,
  point_size = 1,
  center = TRUE,
  title = "Volcano plot"
)
}
\arguments{
\item{devil.res}{A tibble from test_de() containing columns:
\itemize{
\item name: Gene identifiers
\item adj_pval: Adjusted p-values
\item lfc: Log2 fold changes
}}

\item{lfc_cut}{Numeric. Absolute log2 fold change threshold for significance.
Default: 1}

\item{pval_cut}{Numeric. Adjusted p-value threshold for significance.
Default: 0.05}

\item{labels}{Logical. Whether to label genes meeting both significance criteria.
Default: TRUE}

\item{colors}{Character vector of length 4 specifying colors for:
\enumerate{
\item Non-significant genes
\item Fold-change significant only
\item P-value significant only
\item Both significant
Default: c("gray", "forestgreen", "steelblue", "indianred")
}}

\item{color_alpha}{Numeric between 0 and 1. Transparency level for points.
Default: 0.7}

\item{point_size}{Numeric. Size of plotting points.
Default: 1}

\item{center}{Logical. Whether to center the x-axis at zero.
Default: TRUE}

\item{title}{Character. Plot title.
Default: "Volcano plot"}
}
\value{
A ggplot2 object containing the volcano plot.
}
\description{
Generates a customizable volcano plot visualizing differential expression results,
highlighting significant genes based on both fold change and statistical significance.
The plot supports various customization options including color schemes, point sizes,
and gene labeling.
}
\details{
The function creates a scatter plot with:
\itemize{
\item X-axis: Log2 fold change
\item Y-axis: -Log10 adjusted p-value
Points are colored based on significance categories:
}
\enumerate{
\item Non-significant: Neither p-value nor fold change threshold met
\item LFC significant: Only fold change threshold met
\item P-value significant: Only p-value threshold met
\item Both significant: Both thresholds met
}

The plot includes dashed lines indicating significance thresholds and optionally
labels genes meeting both significance criteria.
}
\note{
\itemize{
\item Genes with adj_pval = 0 are assigned the smallest non-zero p-value in the dataset
\item NA values are removed with a warning
\item Gene labels are placed with overlap prevention
}
}
\examples{
\dontrun{
# Basic volcano plot
plot_volcano(de_results)

# Custom thresholds and colors
plot_volcano(de_results,
            lfc_cut = 2,
            pval_cut = 0.01,
            colors = c("grey80", "blue", "green", "red"))

# Without gene labels
plot_volcano(de_results, labels = FALSE)
}

}
