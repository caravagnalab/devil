# edgeR TMM Size Factors

Computes size factors using edgeR's TMM (trimmed mean of M-values)
method with singleton pairing.

## Usage

``` r
edgeR_sf(Y)
```

## Arguments

- Y:

  Count data matrix with genes in rows and samples in columns

## Value

Numeric vector of size factors
