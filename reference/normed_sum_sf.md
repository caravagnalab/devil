# Geometric Mean Normalization Size Factors

Computes size factors based on library sizes (column sums) normalized by
their geometric mean.

## Usage

``` r
normed_sum_sf(Y)
```

## Arguments

- Y:

  Count data matrix with genes in rows and samples in columns

## Value

Numeric vector of size factors
